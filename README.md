Preference files to get you a comfy working environment.

Clone the repo, copy files to appropriate locations - **warning!!!! there is no undo:**

````
cp bashrc ~/.bashrc && cp vimrc ~/.vimrc && cp tmux.conf ~/.tmux.conf
````
